# CloudLab-AI — Development Setup

See the root `README.md` "Local setup" section for the current
Phase 1 setup steps (env vars, install, Postgres/Redis via Docker Compose,
Prisma migrate, running frontend/backend in dev mode).

This document will expand with GitHub OAuth app setup, AI provider API key
setup, and Docker Engine access configuration as those phases land.
